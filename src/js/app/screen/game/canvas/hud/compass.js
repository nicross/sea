app.screen.game.canvas.hud.compass = (() => {
  app.screen.game.canvas.hud.on('draw', draw)

  function draw({canvas, context}) {
    // Get screen coordinates for compass rose points
    // Draw visible elements with decreasing opacity from top-center of screen
  }

  return {}
})()
